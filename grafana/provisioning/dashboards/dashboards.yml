apiVersion: 1

providers:
  - name: 'scanner-dashboards'
    orgId: 1
    folder: 'API Scanner'
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
